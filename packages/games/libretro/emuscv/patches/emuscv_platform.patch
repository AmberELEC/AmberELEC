--- a/Makefile.libretro
+++ b/Makefile.libretro
@@ -113,8 +113,8 @@ ifneq (,$(findstring unix,$(platform)))
 	ifneq (,$(findstring Haiku,$(shell uname -s)))
 		LDFLAGS += -lnetwork -lroot
 	endif
-	CC = gcc
-	CXX = gcc
+	CC ?= gcc
+	CXX ?= g++

