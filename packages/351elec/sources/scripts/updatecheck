#!/bin/bash
# SPDX-License-Identifier: GPL-2.0-or-later
# Copyright (C) 2021-present pkegg

. /etc/profile
DIR=$(realpath $(dirname $0))

if UPDATE=$(python3 ${DIR}/get-release.py --check True 2>/dev/null); then
   echo "${UPDATE}"
else
   echo "no"
   exit 1
fi 